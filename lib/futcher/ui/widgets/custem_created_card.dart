import 'package:flutter/material.dart';

class CustemCreatedCard extends StatelessWidget {
  const CustemCreatedCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}