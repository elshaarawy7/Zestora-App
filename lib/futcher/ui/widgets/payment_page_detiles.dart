import 'package:flutter/widgets.dart';

class PaymentPageDetiles extends StatelessWidget {
  const PaymentPageDetiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}